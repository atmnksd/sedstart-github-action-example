name: Sedstart QA - Local

on:
  workflow_dispatch:

jobs:
  sedstart-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
      - name: Install Playwright Browsers
        run: npx playwright@1.57.0 install chrome --with-deps
      - name: Trigger SedStart test run
        uses: sedstart/runlocal@v1
        with:
          api_key: ${{ secrets.SEDSTART_API_KEY_QA }}
          project_id: 479
          test_id: 2241
          profile_id: 974
          browser: chrome
          headless: false
          environment: QA
